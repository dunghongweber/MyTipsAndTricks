//to log and check object with its variable name by
console.log({ item, product, drink }); //computed property names

//if objects have same properties, can use table to log
console.table([age, name, gender]); //this is best for array of objects
